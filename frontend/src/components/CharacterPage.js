import React, { Component } from "react";

export default class CharacterPage extends Component {
    constructor(props) {
        super(props);
    }

    render() {
        return <p>This is the character page.</p>;
    }
}